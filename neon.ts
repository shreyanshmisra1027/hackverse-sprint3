import { defineConfig } from "@neon/config/v1";

// Authentication is implemented by the Vercel API functions so it can use the
// application's password and message policies. Neon supplies Lakebase Postgres.
export default defineConfig({});
